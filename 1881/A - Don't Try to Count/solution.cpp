#include <bits/stdc++.h>using namespace std; int main() {    ios::sync_with_stdio(false);    cin.tie(nullptr);     int t;    cin >> t;    while (t--) {        int n, m;        cin >> n >> m;         string x, s;        cin >> x >> s;         string cur = x;        int ans = -1;        for (int i = 0; i <= 6; i++) {            if (cur.find(s) != string::npos) {                ans = i;                break;            }            cur += cur;        }         cout << ans << '\n';    }     return 0;}