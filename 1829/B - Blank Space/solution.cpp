#include <bits/stdc++.h>using namespace std; int main() {    ios::sync_with_stdio(false);    cin.tie(NULL);     int t;    cin>>t;    while(t--){      int n;      cin>>n;      vector<int> a(n);      for(int i=0;i<n;i++) {        cin>>a[i];      }      int maxi=0;      int cnt=0;      for(int i=0;i<n;i++){        if(a[i]==1){          cnt=0;        }        if(a[i]==0){          cnt++;          maxi=max(maxi,cnt);         }      }     cout<<maxi<<endl;    }        return 0;}