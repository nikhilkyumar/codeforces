#include <bits/stdc++.h>using namespace std; int main() {    ios::sync_with_stdio(false);    cin.tie(NULL);        int n;        cin>>n;        vector<int>a(n);        for(int i=0;i<n;i++){            cin>>a[i];        }        int ans=abs(a[0]);        for(int i=1;i<n;i++){            ans=min(ans,abs(a[i]));        }        cout<<ans<<'\n';     return 0;}